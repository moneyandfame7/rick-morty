import React, { type FC } from 'react'

import { Subheader } from 'features/users/pages/settings/components'

export const AppearenceContainer: FC = () => {
  return <Subheader title="Theme preferences" />
}
